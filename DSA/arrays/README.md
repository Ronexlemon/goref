# Arrays

In Go, an **array** is a fixed-size collection of elements where all elements have the same type.

## Array Type Syntax

An array type is written as:


Where:
- `n` is the number of elements in the array
- `T` is the type of each element

## Example

```go
var a [10]int
